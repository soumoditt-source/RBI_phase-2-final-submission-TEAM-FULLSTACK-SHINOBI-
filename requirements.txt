# MuleHunter.AI — Production UI Dependencies ONLY (Vercel Node)
# The heavy ML libraries (xgboost, lightgbm, shap) are isolated in requirements-dev.txt 
# as all ML metrics/dossiers are pre-computed for ultra-fast dashboard queries.

streamlit>=1.32.0
pandas>=2.1.0
numpy>=1.26.0
plotly>=5.18.0
pydeck>=0.8.0
